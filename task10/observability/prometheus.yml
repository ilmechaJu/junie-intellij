global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'task10-backend'
    metrics_path: /metrics
    static_configs:
      - targets: ['localhost:3000']
        labels:
          service: task10-backend

rule_files:
  - alerts.yml
